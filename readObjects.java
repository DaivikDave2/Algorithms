class readObjects{
	public static SmartDate[] readSmartDates(){
		Queue<String> queue=new Queue<>();
		while(!StdIn.isEmpty()){
			queue.enqueue(StdIn.readString());
		}
		SmartDate[] dates=new SmartDate[];
		
	}
}